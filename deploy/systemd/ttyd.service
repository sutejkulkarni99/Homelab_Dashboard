[Unit]
Description=ttyd — Web terminal for Polaris
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/ttyd -i /tmp/ttyd.sock -H X-WEBAUTH-USER bash -l
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
